# ClawFinance + OpenClaw — unified environment variables
#
# Quick start:
#   cp .env.example .env
#   Edit .env — fill in the required section and any integrations you want.
#   Then run: bash setup.sh
#
# NEVER commit .env to git.
#
# Sections:
#   1. ClawFinance — required
#   2. ClawFinance — API integrations (optional, enable features as needed)
#   3. OpenClaw — agent engine
#   4. OpenClaw — channels (optional messaging integrations)
#   5. OpenClaw — tools & voice (optional)
#   6. OpenClaw — Docker gateway (only if running OpenClaw in Docker)

# =============================================================================
# 1. CLAWFINANCE — REQUIRED
# =============================================================================

# Postgres password — use a strong random string
DB_PASSWORD=changeme_strong_password

# AES-256 key for encrypting stored API tokens (32+ random chars)
DB_ENCRYPTION_KEY=changeme_very_long_random_passphrase_for_aes256

# Local API key for ClawFinance REST API — any random string works
CLAWFINANCE_API_KEY=changeme_local_api_key

# =============================================================================
# 2. CLAWFINANCE — API INTEGRATIONS (add as needed)
# =============================================================================

# ── Banking: Plaid ──────────────────────────────────────────────────────────
# Get credentials at https://dashboard.plaid.com
# PLAID_ENV=sandbox works for testing without real bank accounts
PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# ── Investments: SnapTrade ───────────────────────────────────────────────────
# Get credentials at https://app.snaptrade.com/partners
SNAPTRADE_CLIENT_ID=
SNAPTRADE_CONSUMER_KEY=

# ── Market Data: Finnhub ─────────────────────────────────────────────────────
# Free tier at https://finnhub.io/register — covers stocks, news, earnings
FINNHUB_API_KEY=

# ── Tax: Azure Document Intelligence ────────────────────────────────────────
# For OCR of W-2s, 1099s, etc. — https://portal.azure.com
AZURE_DOC_INTEL_ENDPOINT=
AZURE_DOC_INTEL_KEY=

# ── Tax: TaxBandits ──────────────────────────────────────────────────────────
# For tax filing — https://taxbandits.com
TAXBANDITS_API_KEY=

# ── Research: Twitter / X API v2 ────────────────────────────────────────────
# Get credentials at https://developer.twitter.com
TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=

# ── Research: SerpAPI (Google Trends, job signals, app rankings) ─────────────
# Free tier at https://serpapi.com
SERPAPI_KEY=

# =============================================================================
# 3. OPENCLAW — AGENT ENGINE
# =============================================================================

# Set at least one model provider API key.
# ClawFinance uses Anthropic (Claude) by default.
ANTHROPIC_API_KEY=

# Other supported providers (uncomment to use)
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=...
# OPENROUTER_API_KEY=sk-or-...

# =============================================================================
# 4. OPENCLAW — CHANNELS (optional messaging integrations)
# =============================================================================

# Receive ClawFinance alerts and chat via your preferred messaging app.
# Only configure the channels you want — all are optional.

# TELEGRAM_BOT_TOKEN=123456:ABCDEF...
# DISCORD_BOT_TOKEN=...
# SLACK_BOT_TOKEN=xoxb-...
# SLACK_APP_TOKEN=xapp-...
# MATTERMOST_BOT_TOKEN=...
# MATTERMOST_URL=https://chat.example.com

# =============================================================================
# 5. OPENCLAW — TOOLS & VOICE (optional)
# =============================================================================

# BRAVE_API_KEY=...
# PERPLEXITY_API_KEY=pplx-...
# FIRECRAWL_API_KEY=...
# ELEVENLABS_API_KEY=...
# DEEPGRAM_API_KEY=...

# =============================================================================
# 6. OPENCLAW — DOCKER GATEWAY (only if running OpenClaw in Docker)
# =============================================================================
#
# Most users run OpenClaw locally from source: pnpm run dev
# These are only needed for: docker compose --profile gateway up

# Recommended if the gateway binds beyond loopback.
# Generate: openssl rand -hex 32
OPENCLAW_GATEWAY_TOKEN=change-me-to-a-long-random-token

# Optional path overrides
# OPENCLAW_CONFIG_DIR=~/.openclaw
# OPENCLAW_WORKSPACE_DIR=.
# OPENCLAW_GATEWAY_BIND=lan
# OPENCLAW_GATEWAY_PORT=18789
# OPENCLAW_BRIDGE_PORT=18790

# Claude session keys (for Claude.ai web mode — usually not needed)
# CLAUDE_AI_SESSION_KEY=...
# CLAUDE_WEB_SESSION_KEY=...
# CLAUDE_WEB_COOKIE=...
